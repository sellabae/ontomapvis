
baselineMappingView = function (ont1root, ont2root, alignments) {
    console.log(`baselineMappingView(ont1root, ont2root, alignments)`);

    //Creates a detached g to return
    const g = d3.create("svg:g");
};