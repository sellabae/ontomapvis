
matrixMappingView = function (ont1root, ont2root, alignments) {
    console.log(`matrixMappingView(ont1root, ont2root, alignments)`);

    //Creates a detached g to return
    const g = d3.create("svg:g");
};